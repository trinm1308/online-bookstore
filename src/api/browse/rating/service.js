const { QueryTypes } = require("sequelize");
const { Sequelize } = require("../../../common/core/sequelize");
const { sequelize } = require("../../../common/core/sequelize");
const RatingService = require("../../manage/rating/service");

module.exports = RatingService;
